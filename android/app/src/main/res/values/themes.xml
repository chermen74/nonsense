<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The whole point of the app: the activity window is translucent, so the
      launcher / whatever is behind it stays visible. The NonsenseView paints a
      faint tint on top so you can tell the toy is active.
    -->
    <style name="Theme.Nonsense.Sheer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
